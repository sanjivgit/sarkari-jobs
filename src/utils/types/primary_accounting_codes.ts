export type PrimaryAccCodes = {
    ulb_id: number | string;
    request_no: string;
    employee_id: number | string;
    date: string;
    group_ref: string;
    code_ref: string;
    description: string;
}